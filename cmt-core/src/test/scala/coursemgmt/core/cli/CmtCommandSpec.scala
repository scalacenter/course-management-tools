package coursemgmt.core.cli

//import caseapp.core.{Indexed, RemainingArgs}
import org.scalatest.wordspec.AnyWordSpecLike
import org.scalatest.matchers.should.Matchers

final class CmtCommandSpec extends AnyWordSpecLike with Matchers {

  "enforceNoTrailingArguments" when {

    "there are trailing arguments" in {
//      RemainingArgs(indexedRemaining = Seq(Indexed.list(List("a"), 0)), indexedUnparsed = )
    }
  }
}
